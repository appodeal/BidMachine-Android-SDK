apply plugin: 'com.android.library'

android {
    defaultConfig {
        versionCode 2
    }
}

apply from: '../adapters.gradle'

dependencies {
    adapterDependency 'com.facebook.android:audience-network-sdk:5.5.0'
}